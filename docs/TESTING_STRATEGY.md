# Testing Strategy

Unit: engines (intelligence, menu, CTA, membership, attendance period, payment, contrast, entitlements).

UI: rail navigation, menu slide, CTA, locked/empty/error.

Screenshot targets: 360×480 and 360×500 dp.

Fixtures live in `preview/scenarios/` only — never inside production composables.
